<H1>Bienvenue dans la partie <%= @game.name %> </H1>

<%= simple_form_for(@avatar) do |f| %>
    <%= f.input :name, label: 'CHOISIS TON PSEUDO'  %>
    <%= hidden_field_tag :game_id, @game.id%>
    <%= f.button :submit, "Rejoindre la partie"%>
  <% end %>
<br>
  <p>Rajouter ici le tuto comment jouer de la HP quand il sera fini</p>
