<h2>Log in</h2>
<br>
<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <br>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { class:"user-input", placeholder:"godefroy.demontmirail@proton.me",autocomplete: "email" }  %>
                <br>
    <%= f.input :password,
                required: false,
                input_html: { class:"user-input", placeholder:"*********", autocomplete: "current-password" } %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>
  <br>
  <div class="form-actions">
    <%= f.button :submit, "Log in", class: "button-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
